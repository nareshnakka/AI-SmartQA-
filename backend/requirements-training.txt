# Optional — for fine-tuning your own QEOS neural model (Phase 3)
# pip install -r requirements-training.txt

torch>=2.1.0
transformers>=4.46.0
peft>=0.13.0
trl>=0.12.0
datasets>=3.1.0
accelerate>=1.1.0
bitsandbytes>=0.44.0
